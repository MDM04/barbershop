import styled from "styled-components";

const Div = styled.div`
  display: flex;
  justify-content: center;
`;

const Payment = () => {
  return <Div>Forma de Pagamento</Div>;
};

export default Payment;
